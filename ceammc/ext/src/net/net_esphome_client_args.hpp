// this is autogenerated file, do not edit!
#ifndef NET_ESPHOME_CLIENT_ARGS_HPP_
#define NET_ESPHOME_CLIENT_ARGS_HPP_

#include "ceammc_atomlist_view.h"
#include "ceammc_object.h"
#include "datatype_color.h"

using namespace ceammc;

namespace {
struct m_connect_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    bool state {true}; // state
    // methods
    int process_state(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!lv[0].isBool()) {
            return INVALID_VALUE;
        }
        // set value
        state = lv[0].asT<bool>();
        // number of matched items
        return 1;
    }
    static const char* arg_state_info() {
        return "STATE (state), bool";
    }
    static const char* usage() {
        return "usage: [connect STATE(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_state_info();
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_state(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[connect( argument #0 'STATE' is required:";
                    Post(obj) << " - " << arg_state_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[connect( argument #0 'STATE' check failed, expected:";
                    Post(obj) << " - " << arg_state_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[connect( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        return true;
    }
};

const char* m_connect_args_info() {
    return "connect to / disconnect from esphome device";
}
void m_connect_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_connect_args_info());
}
struct m_switch_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    t_symbol* key {&s_}; // switch ID
    bool state {false}; // switch state
    // methods
    int process_key(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!lv[0].isSymbol()) {
            return INVALID_VALUE;
        }
        // set value
        key = lv[0].asT<t_symbol*>();
        // number of matched items
        return 1;
    }
    int process_state(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!lv[0].isBool()) {
            return INVALID_VALUE;
        }
        // set value
        state = lv[0].asT<bool>();
        // number of matched items
        return 1;
    }
    static const char* arg_key_info() {
        return "KEY (switch ID), symbol";
    }
    static const char* arg_state_info() {
        return "STATE (switch state), bool";
    }
    static const char* usage() {
        return "usage: [switch KEY STATE(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_key_info();
        Post(obj) << " - " << arg_state_info();
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_key(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[switch( argument #0 'KEY' is required:";
                    Post(obj) << " - " << arg_key_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[switch( argument #0 'KEY' check failed, expected:";
                    Post(obj) << " - " << arg_key_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        matched = process_state(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[switch( argument #1 'STATE' is required:";
                    Post(obj) << " - " << arg_state_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[switch( argument #1 'STATE' check failed, expected:";
                    Post(obj) << " - " << arg_state_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[switch( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        return true;
    }
};

const char* m_switch_args_info() {
    return "turn on/off esphome switch";
}
void m_switch_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_switch_args_info());
}
struct m_text_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    t_symbol* key {&s_}; // sensor ID
    t_symbol* text {&s_}; // sensor text
    // methods
    int process_key(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!lv[0].isSymbol()) {
            return INVALID_VALUE;
        }
        // set value
        key = lv[0].asT<t_symbol*>();
        // number of matched items
        return 1;
    }
    int process_text(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!lv[0].isSymbol()) {
            return INVALID_VALUE;
        }
        // set value
        text = lv[0].asT<t_symbol*>();
        // number of matched items
        return 1;
    }
    static const char* arg_key_info() {
        return "KEY (sensor ID), symbol";
    }
    static const char* arg_text_info() {
        return "TEXT (sensor text), symbol";
    }
    static const char* usage() {
        return "usage: [text KEY TEXT(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_key_info();
        Post(obj) << " - " << arg_text_info();
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_key(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[text( argument #0 'KEY' is required:";
                    Post(obj) << " - " << arg_key_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[text( argument #0 'KEY' check failed, expected:";
                    Post(obj) << " - " << arg_key_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        matched = process_text(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[text( argument #1 'TEXT' is required:";
                    Post(obj) << " - " << arg_text_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[text( argument #1 'TEXT' check failed, expected:";
                    Post(obj) << " - " << arg_text_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[text( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        return true;
    }
};

const char* m_text_args_info() {
    return "set esphome text sensor";
}
void m_text_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_text_args_info());
}
} // namespace 

#endif // NET_ESPHOME_CLIENT_ARGS_HPP_
