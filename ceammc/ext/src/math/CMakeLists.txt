add_custom_target(ceammc_generate_math
    COMMAND ${CMAKE_SOURCE_DIR}/ceammc/py/generate_math.py
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})

set(MATH_SOURCES math_expr.lex math_expr.y)
macro(ceammc_math_external name)
    list(APPEND MATH_C_SOURCES "math_${name}.c")
endmacro()

macro(ceammc_mathxx_external name)
    list(APPEND MATH_CXX_SOURCES "math_${name}.cpp")
endmacro()

list(APPEND MATH_CXX_SOURCES math_bool_op.cpp math_expr_ast.cpp math_sync_base.cpp math_sync_op.cpp)
list(APPEND MATH_C_SOURCES math_expr.tab.c lex.math_expr.c lex.math_expr.h)

# begin autogenerated
ceammc_mathxx_external(abs)
ceammc_mathxx_external(acos)
ceammc_mathxx_external(acosh)
ceammc_mathxx_external(asin)
ceammc_mathxx_external(asinh)
ceammc_mathxx_external(atan)
ceammc_mathxx_external(atanh)
ceammc_mathxx_external(cbrt)
ceammc_mathxx_external(ceil)
ceammc_mathxx_external(cos)
ceammc_mathxx_external(cosh)
ceammc_mathxx_external(erf)
ceammc_mathxx_external(exp)
ceammc_mathxx_external(exp2)
ceammc_mathxx_external(floor)
ceammc_mathxx_external(gamma)
ceammc_mathxx_external(lgamma)
ceammc_mathxx_external(log)
ceammc_mathxx_external(log10)
ceammc_mathxx_external(log2)
ceammc_mathxx_external(round)
ceammc_mathxx_external(sin)
ceammc_mathxx_external(sinh)
ceammc_mathxx_external(sqrt)
ceammc_mathxx_external(tan)
ceammc_mathxx_external(tanh)
ceammc_mathxx_external(trunc)
# constants
ceammc_mathxx_external(e)
ceammc_mathxx_external(nan)
ceammc_mathxx_external(inf)
# end autogenerated

ceammc_mathxx_external(and)
ceammc_mathxx_external(approx)
ceammc_mathxx_external(cabs_tilde)
ceammc_mathxx_external(carg_tilde)
ceammc_mathxx_external(cdiv_tilde)
ceammc_mathxx_external(cexp_tilde)
ceammc_mathxx_external(cmul_tilde)
ceammc_mathxx_external(div)
ceammc_mathxx_external(expr)
ceammc_mathxx_external(gcd)
ceammc_mathxx_external(lcm)
ceammc_mathxx_external(mul)
ceammc_mathxx_external(neg)
ceammc_mathxx_external(or)
ceammc_mathxx_external(pi)
ceammc_mathxx_external(polyeval)
ceammc_mathxx_external(prime)
ceammc_mathxx_external(reciprocal)
ceammc_mathxx_external(sign)
ceammc_mathxx_external(squared)

add_library(ceammc_math STATIC mod_math.h mod_math.cpp ${MATH_SOURCES} ${MATH_C_SOURCES} ${MATH_CXX_SOURCES})

if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
    # gcc boost compile fix
    # set only for c++ source files
    foreach(_cpp ${MATH_CXX_SOURCES})
        set_source_files_properties(${_cpp} PROPERTIES COMPILE_FLAGS -fext-numeric-literals)
    endforeach()
endif()

if(BISON_FOUND AND FLEX_FOUND)
    add_custom_target(math_expr_gen
        COMMAND ${FLEX_EXECUTABLE} --header-file=lex.math_expr.h math_expr.lex
        COMMAND ${BISON_EXECUTABLE} -d math_expr.y
        WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
    )
endif()
