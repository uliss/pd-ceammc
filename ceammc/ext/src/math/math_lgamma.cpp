/// this file is autogenerated, do not edit!
#include <cmath>

#include "ceammc_factory.h"
#include "ceammc_object.h"
#include "ceammc_sound_external.h"

using namespace ceammc;
        

class MathLgamma : public BaseObject
{
    using FloatUnaryFn = t_float(*)(t_float);
public:
    MathLgamma(const PdArgs& a) : BaseObject(a)
    {
        createOutlet();
    }

    void onFloat(t_float f) final
    {
        floatTo(0, std::lgamma(f));
    }

    void onList(const AtomList& l) final
    {
        listTo(0, l.mapFloat(static_cast<FloatUnaryFn>(std::lgamma)));
    }

};


class MathLgammaTilde : public SoundExternal
{
public:
    MathLgammaTilde(const PdArgs& a) : SoundExternal(a)
    {
        createSignalOutlet();
    }

    void processBlock(const t_sample** in, t_sample** out) final
    {
        const auto BS = blockSize();
        for(size_t i = 0; i < BS; i += 8) {
            out[0][i+0] = std::lgamma(in[0][i+0]);
            out[0][i+1] = std::lgamma(in[0][i+1]);
            out[0][i+2] = std::lgamma(in[0][i+2]);
            out[0][i+3] = std::lgamma(in[0][i+3]);
            out[0][i+4] = std::lgamma(in[0][i+4]);
            out[0][i+5] = std::lgamma(in[0][i+5]);
            out[0][i+6] = std::lgamma(in[0][i+6]);
            out[0][i+7] = std::lgamma(in[0][i+7]);
        }
    }
};


void setup_math_lgamma() {
    ObjectFactory<MathLgamma> obj0("math.lgamma");
    SoundExternalFactory<MathLgammaTilde> obj1("math.lgamma~");
}
        
