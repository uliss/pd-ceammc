// this is autogenerated file, do not edit!
#ifndef HW_RPI_SPI_WS2812_ARGS_HPP_
#define HW_RPI_SPI_WS2812_ARGS_HPP_

#include "ceammc_atomlist_view.h"
#include "ceammc_crc32.h"
#include "ceammc_object.h"
#include "datatype_color.h"

using namespace ceammc;

namespace {
CEAMMC_DEFINE_HASH(bounce)
CEAMMC_DEFINE_HASH(breathe)
CEAMMC_DEFINE_HASH(collision)
CEAMMC_DEFINE_HASH(cycle)
CEAMMC_DEFINE_HASH(cylon)
CEAMMC_DEFINE_HASH(fire)
CEAMMC_DEFINE_HASH(meteor)
CEAMMC_DEFINE_HASH(rainbow)
CEAMMC_DEFINE_HASH(sparkle)
CEAMMC_DEFINE_HASH(strobe)
CEAMMC_DEFINE_HASH(twinkle)
struct m_brightness_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    std::uint8_t level {0}; // brightness level
    // methods
    int process_level(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
            return INVALID_VALUE;
        }
        // set value
        level = lv[0].asT<t_int>();
        // number of matched items
        return 1;
    }
    static const char* arg_level_info() {
        return "LEVEL (brightness level), byte[0..255] range";
    }
    static const char* usage() {
        return "usage: [brightness LEVEL(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_level_info();
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_level(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[brightness( argument #0 'LEVEL' is required:";
                    Post(obj) << " - " << arg_level_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[brightness( argument #0 'LEVEL' check failed, expected:";
                    Post(obj) << " - " << arg_level_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[brightness( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        return true;
    }
};

const char* m_brightness_args_info() {
    return "set led strip brighness";
}
void m_brightness_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_brightness_args_info());
}
struct m_clear_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    bool flush {true}; // do flush
    // methods
    int process_flush(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check values
        int take_count = 0;
        const auto N = lv.size();
        if ((0 < N) && lv[0].isBool()) {
            take_count++;
        }
        // set value
        if (take_count == 1) {
            flush = lv[0].asT<bool>();
        }
        // number of matched items
        return take_count;
    }
    static const char* arg_flush_info() {
        return "FLUSH? (do flush), bool";
    }
    static const char* usage() {
        return "usage: [clear FLUSH?(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_flush_info();
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_flush(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[clear( argument #0 'FLUSH?' is required:";
                    Post(obj) << " - " << arg_flush_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[clear( argument #0 'FLUSH?' check failed, expected:";
                    Post(obj) << " - " << arg_flush_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[clear( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        return true;
    }
};

const char* m_clear_args_info() {
    return "clear the internal pixel buffer and (optionally) flush it to the strip";
}
void m_clear_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_clear_args_info());
}
struct m_set_pixel_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    t_int pos {0}; // pixel position
    // props
    enum class PropProcessState { Ok, NotFound, InvalidValue };
    // types
    struct prop_color8_t {
        int _count {0};
        std::uint8_t red {0}; // red color component
        std::uint8_t green {0}; // green color component
        std::uint8_t blue {0}; // blue color component
        int process_red(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            red = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_green(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            green = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_blue(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            blue = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        static const char* arg_red_info() {
            return "RED (red color component), byte[0..255] range";
        }
        static const char* arg_green_info() {
            return "GREEN (green color component), byte[0..255] range";
        }
        static const char* arg_blue_info() {
            return "BLUE (blue color component), byte[0..255] range";
        }
        static const char* info() {
            return "@color8 RED GREEN BLUE (RGB int pixel color in range [0..255])";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_red(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_green(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_blue(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[set_pixel @color8( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color_t {
        int _count {0};
        DataTypeColor color {}; // pixel color
        int process_color(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            if ((lv.size() == 3) && lv[0].isFloat() && (0 <= lv[0].asT<t_float>()) && (lv[0].asT<t_float>() <= 1) && lv[1].isFloat() && (0 <= lv[1].asT<t_float>()) && (lv[1].asT<t_float>() <= 1) && lv[2].isFloat() && (0 <= lv[2].asT<t_float>()) && (lv[2].asT<t_float>() <= 1)) {
                // check rgb triplet
                color.setRed(lv[0].asT<t_float>());
                color.setGreen(lv[1].asT<t_float>());
                color.setRed(lv[2].asT<t_float>());
                return 3;
            } else if ((lv.size() == 1) && lv[0].isSymbol() && (lv[0].asT<t_symbol*>()->s_name[0] == '#')) {
                if (DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid hex value: '" << lv[0] << "'";
                    }
                    if (print_err) {
                        Post(obj) << "expected #XXXXXX or #XXX format, for example: #00FFAA or #D0F";
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv[0].isSymbol()) {
                if (DataTypeColor::hasNamedColor(lv[0].asT<t_symbol*>()->s_name) && DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid named color: '" << lv[0] << "'";
                        Error(obj) << "expected: " << DataTypeColor::namedColorList();
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv.isA<DataTypeColor>()) {
                color = *lv[0].asD<DataTypeColor>();
                return 1;
            } else {
                return INVALID_VALUE;
            }
        }
        static const char* arg_color_info() {
            return "COLOR (pixel color), colorRGB color in various formats: float RBG triplet, hex string, named color or color data atom";
        }
        static const char* info() {
            return "@color COLOR (float RGB color list, hex, named color or color datatype)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_color(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[set_pixel @color( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    // vars
    prop_color8_t prop_color8; // RGB int pixel color in range [0..255]
    prop_color_t prop_color; // float RGB color list, hex, named color or color datatype
    // methods
    int process_pos(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!(lv[0].isInteger() && (lv[0].asT<t_int>() >= 0))) {
            return INVALID_VALUE;
        }
        // set value
        pos = lv[0].asT<t_int>();
        // number of matched items
        return 1;
    }
    PropProcessState process_prop_color8 (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color8"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color8.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color8._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color._count++;
        return PropProcessState::Ok;
    }
    static const char* arg_pos_info() {
        return "POS (pixel position), int >= 0";
    }
    static const char* usage() {
        return "usage: [set_pixel POS ^@color8 ^@color(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_pos_info();
        Post(obj) << " - " << prop_color8_t::info();
        Post(obj) << " - " << prop_color_t::info();
    }
    bool check_xor_props(const BaseObject* obj, bool print_err) const {
        if (prop_color._count > 0 && prop_color8._count > 0) {
            if (print_err) {
                Error(obj) << "[set_pixel( the properties @color and @color8 cannot be used at the same time";
            }
            return false;
        } else if (prop_color._count == 0 && prop_color8._count == 0) {
            if (print_err) {
                Error(obj) << "[set_pixel( one of this properties is required: @color or @color8";
            }
            return false;
        }
        return true;
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_pos(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[set_pixel( argument #0 'POS' is required:";
                    Post(obj) << " - " << arg_pos_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[set_pixel( argument #0 'POS' check failed, expected:";
                    Post(obj) << " - " << arg_pos_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[set_pixel( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        // check properties
        PropProcessState prop_st = PropProcessState::Ok;
        prop_st = process_prop_color8(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[set_pixel( invalid value for @color8 property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color8_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color8._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color8 properties are specified";
                    Error(obj) << "only 1 ^(@color) entries for property @color8 are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[set_pixel( invalid value for @color property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color properties are specified";
                    Error(obj) << "only 1 ^(@color8) entries for property @color are expected";
                }
                return false;
            }
        }
        if (!check_xor_props(obj, print_err)) {
            return false;
        }
        return true;
    }
};

const char* m_set_pixel_args_info() {
    return "set pixel color in the internal buffer";
}
void m_set_pixel_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_set_pixel_args_info());
}
struct m_fill_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // props
    enum class PropProcessState { Ok, NotFound, InvalidValue };
    // types
    struct prop_color_t {
        int _count {0};
        DataTypeColor color {}; // fill color
        int process_color(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            if ((lv.size() == 3) && lv[0].isFloat() && (0 <= lv[0].asT<t_float>()) && (lv[0].asT<t_float>() <= 1) && lv[1].isFloat() && (0 <= lv[1].asT<t_float>()) && (lv[1].asT<t_float>() <= 1) && lv[2].isFloat() && (0 <= lv[2].asT<t_float>()) && (lv[2].asT<t_float>() <= 1)) {
                // check rgb triplet
                color.setRed(lv[0].asT<t_float>());
                color.setGreen(lv[1].asT<t_float>());
                color.setRed(lv[2].asT<t_float>());
                return 3;
            } else if ((lv.size() == 1) && lv[0].isSymbol() && (lv[0].asT<t_symbol*>()->s_name[0] == '#')) {
                if (DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid hex value: '" << lv[0] << "'";
                    }
                    if (print_err) {
                        Post(obj) << "expected #XXXXXX or #XXX format, for example: #00FFAA or #D0F";
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv[0].isSymbol()) {
                if (DataTypeColor::hasNamedColor(lv[0].asT<t_symbol*>()->s_name) && DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid named color: '" << lv[0] << "'";
                        Error(obj) << "expected: " << DataTypeColor::namedColorList();
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv.isA<DataTypeColor>()) {
                color = *lv[0].asD<DataTypeColor>();
                return 1;
            } else {
                return INVALID_VALUE;
            }
        }
        static const char* arg_color_info() {
            return "COLOR (fill color), colorRGB color in various formats: float RBG triplet, hex string, named color or color data atom";
        }
        static const char* info() {
            return "@color COLOR (RGB color)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_color(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill @color( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color8_t {
        int _count {0};
        std::uint8_t red {0}; // red color component
        std::uint8_t green {0}; // green color component
        std::uint8_t blue {0}; // blue color component
        int process_red(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            red = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_green(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            green = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_blue(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            blue = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        static const char* arg_red_info() {
            return "RED (red color component), byte[0..255] range";
        }
        static const char* arg_green_info() {
            return "GREEN (green color component), byte[0..255] range";
        }
        static const char* arg_blue_info() {
            return "BLUE (blue color component), byte[0..255] range";
        }
        static const char* info() {
            return "@color8 RED GREEN BLUE (int RGB color in [0..255] range)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_red(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_green(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_blue(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill @color8( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    // vars
    prop_color_t prop_color; // RGB color
    prop_color8_t prop_color8; // int RGB color in [0..255] range
    // methods
    PropProcessState process_prop_color (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color8 (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color8"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color8.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color8._count++;
        return PropProcessState::Ok;
    }
    static const char* usage() {
        return "usage: [fill ^@color ^@color8(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << prop_color_t::info();
        Post(obj) << " - " << prop_color8_t::info();
    }
    bool check_xor_props(const BaseObject* obj, bool print_err) const {
        if (prop_color8._count > 0 && prop_color._count > 0) {
            if (print_err) {
                Error(obj) << "[fill( the properties @color8 and @color cannot be used at the same time";
            }
            return false;
        } else if (prop_color8._count == 0 && prop_color._count == 0) {
            if (print_err) {
                Error(obj) << "[fill( one of this properties is required: @color8 or @color";
            }
            return false;
        }
        return true;
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[fill( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        // check properties
        PropProcessState prop_st = PropProcessState::Ok;
        prop_st = process_prop_color(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill( invalid value for @color property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color properties are specified";
                    Error(obj) << "only 1 ^(@color8) entries for property @color are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color8(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill( invalid value for @color8 property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color8_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color8._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color8 properties are specified";
                    Error(obj) << "only 1 ^(@color) entries for property @color8 are expected";
                }
                return false;
            }
        }
        if (!check_xor_props(obj, print_err)) {
            return false;
        }
        return true;
    }
};

const char* m_fill_args_info() {
    return "fill all pixels in the internal buffer with specified color";
}
void m_fill_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_fill_args_info());
}
struct m_fill_lslice_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // props
    enum class PropProcessState { Ok, NotFound, InvalidValue };
    // types
    struct prop_lslice_t {
        int _count {0};
        t_int start {0}; // start index, can be negative. If negative: means position from the end of the buffer
        t_int length {0}; // slice length. If ommitted means position from the end of the buffer
        t_int step {1}; // step between pixels
        int process_start(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!lv[0].isInteger()) {
                return INVALID_VALUE;
            }
            // set value
            start = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_length(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger() && (lv[0].asT<t_int>() > 0)) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                length = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        int process_step(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger() && (lv[0].asT<t_int>() > 0)) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                step = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        static const char* arg_start_info() {
            return "START (start index, can be negative. If negative: means position from the end of the buffer), int";
        }
        static const char* arg_length_info() {
            return "LENGTH? (slice length. If ommitted means position from the end of the buffer), int > 0";
        }
        static const char* arg_step_info() {
            return "STEP? (step between pixels), int > 0";
        }
        static const char* info() {
            return "@lslice START LENGTH? STEP? (length-based pixel slice)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_start(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_length(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_step(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_lslice @lslice( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color_t {
        int _count {0};
        DataTypeColor color {}; // 
        int process_color(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            if ((lv.size() == 3) && lv[0].isFloat() && (0 <= lv[0].asT<t_float>()) && (lv[0].asT<t_float>() <= 1) && lv[1].isFloat() && (0 <= lv[1].asT<t_float>()) && (lv[1].asT<t_float>() <= 1) && lv[2].isFloat() && (0 <= lv[2].asT<t_float>()) && (lv[2].asT<t_float>() <= 1)) {
                // check rgb triplet
                color.setRed(lv[0].asT<t_float>());
                color.setGreen(lv[1].asT<t_float>());
                color.setRed(lv[2].asT<t_float>());
                return 3;
            } else if ((lv.size() == 1) && lv[0].isSymbol() && (lv[0].asT<t_symbol*>()->s_name[0] == '#')) {
                if (DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid hex value: '" << lv[0] << "'";
                    }
                    if (print_err) {
                        Post(obj) << "expected #XXXXXX or #XXX format, for example: #00FFAA or #D0F";
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv[0].isSymbol()) {
                if (DataTypeColor::hasNamedColor(lv[0].asT<t_symbol*>()->s_name) && DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid named color: '" << lv[0] << "'";
                        Error(obj) << "expected: " << DataTypeColor::namedColorList();
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv.isA<DataTypeColor>()) {
                color = *lv[0].asD<DataTypeColor>();
                return 1;
            } else {
                return INVALID_VALUE;
            }
        }
        static const char* arg_color_info() {
            return "COLOR (), colorRGB color in various formats: float RBG triplet, hex string, named color or color data atom";
        }
        static const char* info() {
            return "@color COLOR (RGB color)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_color(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_lslice @color( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color8_t {
        int _count {0};
        std::uint8_t red {0}; // red color component
        std::uint8_t green {0}; // green color component
        std::uint8_t blue {0}; // blue color component
        int process_red(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            red = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_green(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            green = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_blue(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            blue = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        static const char* arg_red_info() {
            return "RED (red color component), byte[0..255] range";
        }
        static const char* arg_green_info() {
            return "GREEN (green color component), byte[0..255] range";
        }
        static const char* arg_blue_info() {
            return "BLUE (blue color component), byte[0..255] range";
        }
        static const char* info() {
            return "@color8 RED GREEN BLUE (int RGB color in [0..255] range)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_red(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_green(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_blue(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_lslice @color8( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    // vars
    prop_lslice_t prop_lslice; // length-based pixel slice
    prop_color_t prop_color; // RGB color
    prop_color8_t prop_color8; // int RGB color in [0..255] range
    // methods
    PropProcessState process_prop_lslice (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@lslice"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_lslice.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_lslice._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color8 (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color8"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color8.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color8._count++;
        return PropProcessState::Ok;
    }
    static const char* usage() {
        return "usage: [fill_lslice @lslice ^@color ^@color8(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << prop_lslice_t::info();
        Post(obj) << " - " << prop_color_t::info();
        Post(obj) << " - " << prop_color8_t::info();
    }
    bool check_xor_props(const BaseObject* obj, bool print_err) const {
        if (prop_color8._count > 0 && prop_color._count > 0) {
            if (print_err) {
                Error(obj) << "[fill_lslice( the properties @color8 and @color cannot be used at the same time";
            }
            return false;
        } else if (prop_color8._count == 0 && prop_color._count == 0) {
            if (print_err) {
                Error(obj) << "[fill_lslice( one of this properties is required: @color8 or @color";
            }
            return false;
        }
        return true;
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[fill_lslice( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        // check properties
        PropProcessState prop_st = PropProcessState::Ok;
        prop_st = process_prop_lslice(lv, obj, print_err);
        if (prop_st == PropProcessState::NotFound) {
            if (prop_lslice._count < 1) {
                if (print_err) {
                    Error(obj) << "[fill_lslice( property @lslice is required";;
                    Post(obj) << " - " << prop_lslice_t::info();
                }
                return false;
            }
        }
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_lslice( invalid value for @lslice property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_lslice_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_lslice._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @lslice properties are specified";
                    Error(obj) << "only 1 entries for property @lslice are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_lslice( invalid value for @color property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color properties are specified";
                    Error(obj) << "only 1 ^(@color8) entries for property @color are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color8(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_lslice( invalid value for @color8 property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color8_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color8._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color8 properties are specified";
                    Error(obj) << "only 1 ^(@color) entries for property @color8 are expected";
                }
                return false;
            }
        }
        if (!check_xor_props(obj, print_err)) {
            return false;
        }
        return true;
    }
};

const char* m_fill_lslice_args_info() {
    return "fill the range of pixels in the internal buffer with specified color";
}
void m_fill_lslice_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_fill_lslice_args_info());
}
struct m_fill_pixels_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // props
    enum class PropProcessState { Ok, NotFound, InvalidValue };
    // types
    struct prop_indexes_t {
        int _count {0};
        AtomListView idx; // pixel index, can be negative. If negative: means position from the end of the buffer
        int process_idx(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            int take_count = 0;
            for (auto& a: lv) {
                if (a.isInteger()) {
                    take_count++;
                }
            else {
                break;
            }
            }
            // check count
            if (take_count < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // set value
            idx = lv.subView(0, take_count);
            // number of matched items
            return take_count;
        }
        static const char* arg_idx_info() {
            return "IDX+ (pixel index, can be negative. If negative: means position from the end of the buffer), int";
        }
        static const char* info() {
            return "@indexes IDX+ (list of pixel positions)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_idx(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_pixels @indexes( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color_t {
        int _count {0};
        DataTypeColor color {}; // 
        int process_color(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            if ((lv.size() == 3) && lv[0].isFloat() && (0 <= lv[0].asT<t_float>()) && (lv[0].asT<t_float>() <= 1) && lv[1].isFloat() && (0 <= lv[1].asT<t_float>()) && (lv[1].asT<t_float>() <= 1) && lv[2].isFloat() && (0 <= lv[2].asT<t_float>()) && (lv[2].asT<t_float>() <= 1)) {
                // check rgb triplet
                color.setRed(lv[0].asT<t_float>());
                color.setGreen(lv[1].asT<t_float>());
                color.setRed(lv[2].asT<t_float>());
                return 3;
            } else if ((lv.size() == 1) && lv[0].isSymbol() && (lv[0].asT<t_symbol*>()->s_name[0] == '#')) {
                if (DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid hex value: '" << lv[0] << "'";
                    }
                    if (print_err) {
                        Post(obj) << "expected #XXXXXX or #XXX format, for example: #00FFAA or #D0F";
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv[0].isSymbol()) {
                if (DataTypeColor::hasNamedColor(lv[0].asT<t_symbol*>()->s_name) && DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid named color: '" << lv[0] << "'";
                        Error(obj) << "expected: " << DataTypeColor::namedColorList();
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv.isA<DataTypeColor>()) {
                color = *lv[0].asD<DataTypeColor>();
                return 1;
            } else {
                return INVALID_VALUE;
            }
        }
        static const char* arg_color_info() {
            return "COLOR (), colorRGB color in various formats: float RBG triplet, hex string, named color or color data atom";
        }
        static const char* info() {
            return "@color COLOR (RGB color)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_color(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_pixels @color( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color8_t {
        int _count {0};
        std::uint8_t red {0}; // red color component
        std::uint8_t green {0}; // green color component
        std::uint8_t blue {0}; // blue color component
        int process_red(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            red = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_green(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            green = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_blue(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            blue = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        static const char* arg_red_info() {
            return "RED (red color component), byte[0..255] range";
        }
        static const char* arg_green_info() {
            return "GREEN (green color component), byte[0..255] range";
        }
        static const char* arg_blue_info() {
            return "BLUE (blue color component), byte[0..255] range";
        }
        static const char* info() {
            return "@color8 RED GREEN BLUE (int RGB color in [0..255] range)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_red(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_green(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_blue(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_pixels @color8( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    // vars
    prop_indexes_t prop_indexes; // list of pixel positions
    prop_color_t prop_color; // RGB color
    prop_color8_t prop_color8; // int RGB color in [0..255] range
    // methods
    PropProcessState process_prop_indexes (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@indexes"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_indexes.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_indexes._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color8 (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color8"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color8.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color8._count++;
        return PropProcessState::Ok;
    }
    static const char* usage() {
        return "usage: [fill_pixels @indexes ^@color ^@color8(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << prop_indexes_t::info();
        Post(obj) << " - " << prop_color_t::info();
        Post(obj) << " - " << prop_color8_t::info();
    }
    bool check_xor_props(const BaseObject* obj, bool print_err) const {
        if (prop_color8._count > 0 && prop_color._count > 0) {
            if (print_err) {
                Error(obj) << "[fill_pixels( the properties @color8 and @color cannot be used at the same time";
            }
            return false;
        } else if (prop_color8._count == 0 && prop_color._count == 0) {
            if (print_err) {
                Error(obj) << "[fill_pixels( one of this properties is required: @color8 or @color";
            }
            return false;
        }
        return true;
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[fill_pixels( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        // check properties
        PropProcessState prop_st = PropProcessState::Ok;
        prop_st = process_prop_indexes(lv, obj, print_err);
        if (prop_st == PropProcessState::NotFound) {
            if (prop_indexes._count < 1) {
                if (print_err) {
                    Error(obj) << "[fill_pixels( property @indexes is required";;
                    Post(obj) << " - " << prop_indexes_t::info();
                }
                return false;
            }
        }
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_pixels( invalid value for @indexes property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_indexes_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_indexes._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @indexes properties are specified";
                    Error(obj) << "only 1 entries for property @indexes are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_pixels( invalid value for @color property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color properties are specified";
                    Error(obj) << "only 1 ^(@color8) entries for property @color are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color8(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_pixels( invalid value for @color8 property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color8_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color8._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color8 properties are specified";
                    Error(obj) << "only 1 ^(@color) entries for property @color8 are expected";
                }
                return false;
            }
        }
        if (!check_xor_props(obj, print_err)) {
            return false;
        }
        return true;
    }
};

const char* m_fill_pixels_args_info() {
    return "fill the range of pixels in the internal buffer with specified color";
}
void m_fill_pixels_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_fill_pixels_args_info());
}
struct m_fill_slice_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // props
    enum class PropProcessState { Ok, NotFound, InvalidValue };
    // types
    struct prop_slice_t {
        int _count {0};
        t_int first {0}; // start index, can be negative. If negative: means position from the end of the buffer
        t_int last {-1}; // last index, can be negative. If negative: means position from the end of the buffer
        t_int step {1}; // step between pixels
        int process_first(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!lv[0].isInteger()) {
                return INVALID_VALUE;
            }
            // set value
            first = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_last(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger()) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                last = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        int process_step(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger() && (lv[0].asT<t_int>() > 0)) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                step = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        static const char* arg_first_info() {
            return "FIRST (start index, can be negative. If negative: means position from the end of the buffer), int";
        }
        static const char* arg_last_info() {
            return "LAST? (last index, can be negative. If negative: means position from the end of the buffer), int";
        }
        static const char* arg_step_info() {
            return "STEP? (step between pixels), int > 0";
        }
        static const char* info() {
            return "@slice FIRST LAST? STEP? (range-based pixel slice)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_first(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_last(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_step(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_slice @slice( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color_t {
        int _count {0};
        DataTypeColor color {}; // 
        int process_color(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            if ((lv.size() == 3) && lv[0].isFloat() && (0 <= lv[0].asT<t_float>()) && (lv[0].asT<t_float>() <= 1) && lv[1].isFloat() && (0 <= lv[1].asT<t_float>()) && (lv[1].asT<t_float>() <= 1) && lv[2].isFloat() && (0 <= lv[2].asT<t_float>()) && (lv[2].asT<t_float>() <= 1)) {
                // check rgb triplet
                color.setRed(lv[0].asT<t_float>());
                color.setGreen(lv[1].asT<t_float>());
                color.setRed(lv[2].asT<t_float>());
                return 3;
            } else if ((lv.size() == 1) && lv[0].isSymbol() && (lv[0].asT<t_symbol*>()->s_name[0] == '#')) {
                if (DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid hex value: '" << lv[0] << "'";
                    }
                    if (print_err) {
                        Post(obj) << "expected #XXXXXX or #XXX format, for example: #00FFAA or #D0F";
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv[0].isSymbol()) {
                if (DataTypeColor::hasNamedColor(lv[0].asT<t_symbol*>()->s_name) && DataTypeColor::parseFromList(lv, color)) {
                    return 1;
                } else {
                    if (print_err) {
                        Error(obj) << "invalid named color: '" << lv[0] << "'";
                        Error(obj) << "expected: " << DataTypeColor::namedColorList();
                    }
                    return INVALID_VALUE;
                }
            } else if ((lv.size() == 1) && lv.isA<DataTypeColor>()) {
                color = *lv[0].asD<DataTypeColor>();
                return 1;
            } else {
                return INVALID_VALUE;
            }
        }
        static const char* arg_color_info() {
            return "COLOR (), colorRGB color in various formats: float RBG triplet, hex string, named color or color data atom";
        }
        static const char* info() {
            return "@color COLOR (RGB color)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_color(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_slice @color( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_color8_t {
        int _count {0};
        std::uint8_t red {0}; // red color component
        std::uint8_t green {0}; // green color component
        std::uint8_t blue {0}; // blue color component
        int process_red(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            red = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_green(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            green = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_blue(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!(lv[0].isInteger() && lv[0].isIntInClosedInterval(0, 255))) {
                return INVALID_VALUE;
            }
            // set value
            blue = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        static const char* arg_red_info() {
            return "RED (red color component), byte[0..255] range";
        }
        static const char* arg_green_info() {
            return "GREEN (green color component), byte[0..255] range";
        }
        static const char* arg_blue_info() {
            return "BLUE (blue color component), byte[0..255] range";
        }
        static const char* info() {
            return "@color8 RED GREEN BLUE (int RGB color in [0..255] range)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_red(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_green(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_blue(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fill_slice @color8( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    // vars
    prop_slice_t prop_slice; // range-based pixel slice
    prop_color_t prop_color; // RGB color
    prop_color8_t prop_color8; // int RGB color in [0..255] range
    // methods
    PropProcessState process_prop_slice (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@slice"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_slice.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_slice._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_color8 (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@color8"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_color8.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_color8._count++;
        return PropProcessState::Ok;
    }
    static const char* usage() {
        return "usage: [fill_slice @slice ^@color ^@color8(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << prop_slice_t::info();
        Post(obj) << " - " << prop_color_t::info();
        Post(obj) << " - " << prop_color8_t::info();
    }
    bool check_xor_props(const BaseObject* obj, bool print_err) const {
        if (prop_color8._count > 0 && prop_color._count > 0) {
            if (print_err) {
                Error(obj) << "[fill_slice( the properties @color8 and @color cannot be used at the same time";
            }
            return false;
        } else if (prop_color8._count == 0 && prop_color._count == 0) {
            if (print_err) {
                Error(obj) << "[fill_slice( one of this properties is required: @color8 or @color";
            }
            return false;
        }
        return true;
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[fill_slice( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        // check properties
        PropProcessState prop_st = PropProcessState::Ok;
        prop_st = process_prop_slice(lv, obj, print_err);
        if (prop_st == PropProcessState::NotFound) {
            if (prop_slice._count < 1) {
                if (print_err) {
                    Error(obj) << "[fill_slice( property @slice is required";;
                    Post(obj) << " - " << prop_slice_t::info();
                }
                return false;
            }
        }
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_slice( invalid value for @slice property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_slice_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_slice._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @slice properties are specified";
                    Error(obj) << "only 1 entries for property @slice are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_slice( invalid value for @color property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color properties are specified";
                    Error(obj) << "only 1 ^(@color8) entries for property @color are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_color8(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fill_slice( invalid value for @color8 property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_color8_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_color8._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @color8 properties are specified";
                    Error(obj) << "only 1 ^(@color) entries for property @color8 are expected";
                }
                return false;
            }
        }
        if (!check_xor_props(obj, print_err)) {
            return false;
        }
        return true;
    }
};

const char* m_fill_slice_args_info() {
    return "fill the range of pixels in the internal buffer with specified color";
}
void m_fill_slice_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_fill_slice_args_info());
}
struct m_fx_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    t_symbol* fx_name {&s_}; // effect name
    // props
    enum class PropProcessState { Ok, NotFound, InvalidValue };
    // types
    struct prop_slice_t {
        int _count {0};
        t_int first {0}; // start index, can be negative. If negative: means position from the end of the buffer
        t_int last {-1}; // last index, can be negative. If negative: means position from the end of the buffer
        t_int step {1}; // step between pixels
        int process_first(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!lv[0].isInteger()) {
                return INVALID_VALUE;
            }
            // set value
            first = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_last(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger()) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                last = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        int process_step(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger() && (lv[0].asT<t_int>() > 0)) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                step = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        static const char* arg_first_info() {
            return "FIRST (start index, can be negative. If negative: means position from the end of the buffer), int";
        }
        static const char* arg_last_info() {
            return "LAST? (last index, can be negative. If negative: means position from the end of the buffer), int";
        }
        static const char* arg_step_info() {
            return "STEP? (step between pixels), int > 0";
        }
        static const char* info() {
            return "@slice FIRST LAST? STEP? (range-based pixel slice)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_first(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_last(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_step(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fx @slice( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    struct prop_lslice_t {
        int _count {0};
        t_int first {0}; // start index, can be negative. If negative: means position from the end of the buffer
        t_int length {0}; // length. If 0 or not specified: apply to all pixels
        t_int step {1}; // step between pixels
        int process_first(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check size
            if (lv.size() < 1) {
                return NOT_ENOUGH_ARGS;
            }
            // check values
            if (!lv[0].isInteger()) {
                return INVALID_VALUE;
            }
            // set value
            first = lv[0].asT<t_int>();
            // number of matched items
            return 1;
        }
        int process_length(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger() && (lv[0].asT<t_int>() >= 0)) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                length = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        int process_step(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            // check values
            int take_count = 0;
            const auto N = lv.size();
            if ((0 < N) && lv[0].isInteger() && (lv[0].asT<t_int>() > 0)) {
                take_count++;
            }
            // set value
            if (take_count == 1) {
                step = lv[0].asT<t_int>();
            }
            // number of matched items
            return take_count;
        }
        static const char* arg_first_info() {
            return "FIRST (start index, can be negative. If negative: means position from the end of the buffer), int";
        }
        static const char* arg_length_info() {
            return "LENGTH? (length. If 0 or not specified: apply to all pixels), int >= 0";
        }
        static const char* arg_step_info() {
            return "STEP? (step between pixels), int > 0";
        }
        static const char* info() {
            return "@lslice FIRST LENGTH? STEP? (length-based pixel slice)";
        }
        bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err) {
            int matched = 0;
            AtomListView left_args = lv.arguments();
            matched = process_first(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_length(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            matched = process_step(left_args, obj, print_err);
            if (matched >= 0) {
                left_args = left_args.subView(matched);
            } else {
                return false;
            }
            // check extra arguments
            if (left_args.size()) {
                if (print_err) {
                    Error(obj) << "[fx @lslice( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                    output_usage(obj);
                }
                return false;
            }
            return true;
        }
    };
    // vars
    prop_slice_t prop_slice; // range-based pixel slice
    prop_lslice_t prop_lslice; // length-based pixel slice
    // methods
    int process_fx_name(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!(lv[0].isSymbol() || (strcmp(lv[0].asT<t_symbol*>()->s_name, "rainbow") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "breathe") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "bounce") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "collision") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "cycle") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "cylon") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "fire") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "meteor") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "sparkle") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "twinkle") == 0) || (strcmp(lv[0].asT<t_symbol*>()->s_name, "strobe") == 0))) {
            return INVALID_VALUE;
        }
        // set value
        fx_name = lv[0].asT<t_symbol*>();
        // number of matched items
        return 1;
    }
    PropProcessState process_prop_slice (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@slice"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_slice.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_slice._count++;
        return PropProcessState::Ok;
    }
    PropProcessState process_prop_lslice (const AtomListView& lv, const BaseObject* obj, bool print_err) {
        AtomListView prop;
        if (!lv.getProperty(gensym("@lslice"), prop)) {
            return PropProcessState::NotFound;
        }
        if (!prop_lslice.parse_args(prop, obj, print_err)) {
            return PropProcessState::InvalidValue;
        }
        prop_lslice._count++;
        return PropProcessState::Ok;
    }
    static const char* arg_fx_name_info() {
        return "FX_NAME (effect name), symbol ('bounce', 'breathe', 'collision', 'cycle', 'cylon', 'fire', 'meteor', 'rainbow', 'sparkle', 'strobe', 'twinkle')";
    }
    static const char* usage() {
        return "usage: [fx FX_NAME ^@slice ^@lslice(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_fx_name_info();
        Post(obj) << " - " << prop_slice_t::info();
        Post(obj) << " - " << prop_lslice_t::info();
    }
    bool check_xor_props(const BaseObject* obj, bool print_err) const {
        if (prop_lslice._count > 0 && prop_slice._count > 0) {
            if (print_err) {
                Error(obj) << "[fx( the properties @lslice and @slice cannot be used at the same time";
            }
            return false;
        } else if (prop_lslice._count == 0 && prop_slice._count == 0) {
            if (print_err) {
                Error(obj) << "[fx( one of this properties is required: @lslice or @slice";
            }
            return false;
        }
        return true;
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_fx_name(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[fx( argument #0 'FX_NAME' is required:";
                    Post(obj) << " - " << arg_fx_name_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[fx( argument #0 'FX_NAME' check failed, expected:";
                    Post(obj) << " - " << arg_fx_name_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[fx( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        // check properties
        PropProcessState prop_st = PropProcessState::Ok;
        prop_st = process_prop_slice(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fx( invalid value for @slice property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_slice_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_slice._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @slice properties are specified";
                    Error(obj) << "only 1 ^(@lslice) entries for property @slice are expected";
                }
                return false;
            }
        }
        prop_st = process_prop_lslice(lv, obj, print_err);
        if (prop_st == PropProcessState::InvalidValue) {
            if (print_err) {
                Error(obj) << "[fx( invalid value for @lslice property, expected:";
            }
            if (print_err) {
                Post(obj) << prop_lslice_t::info();
            }
            return false;
        } else if (prop_st == PropProcessState::Ok) {
            if (prop_lslice._count > 1) {
                if (print_err) {
                    Error(obj) << "too many @lslice properties are specified";
                    Error(obj) << "only 1 ^(@slice) entries for property @lslice are expected";
                }
                return false;
            }
        }
        if (!check_xor_props(obj, print_err)) {
            return false;
        }
        return true;
    }
};

const char* m_fx_args_info() {
    return "apply fx to internal buffer";
}
void m_fx_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_fx_args_info());
}
struct m_rotate_args {
    enum ArgProcessState { NOT_ENOUGH_ARGS = -3, INVALID_VALUE = -1 };
    // args
    t_int n {0}; // rotation amount
    // methods
    int process_n(const AtomListView& lv, const BaseObject* obj, bool print_err) {
        // check size
        if (lv.size() < 1) {
            return NOT_ENOUGH_ARGS;
        }
        // check values
        if (!lv[0].isInteger()) {
            return INVALID_VALUE;
        }
        // set value
        n = lv[0].asT<t_int>();
        // number of matched items
        return 1;
    }
    static const char* arg_n_info() {
        return "N (rotation amount), int";
    }
    static const char* usage() {
        return "usage: [rotate N(";
    }
    static void output_usage(const BaseObject* obj) {
        Post(obj) << usage();
    }
    static void output_usage_verbose(const BaseObject* obj) {
        Error(obj) << usage() << " where:";
        Post(obj) << " - " << arg_n_info();
    }
    bool parse_args(const AtomListView& lv, const BaseObject* obj, bool print_err = true) {
        int matched = 0;
        AtomListView left_args = lv.arguments();
        matched = process_n(left_args, obj, print_err);
        if (matched >= 0) {
            left_args = left_args.subView(matched);
        } else {
            if (print_err) {
                if (matched == NOT_ENOUGH_ARGS) {
                    Error(obj) << "[rotate( argument #0 'N' is required:";
                    Post(obj) << " - " << arg_n_info();
                    output_usage(obj);
                } else if (matched == INVALID_VALUE) {
                    Error(obj) << "[rotate( argument #0 'N' check failed, expected:";
                    Post(obj) << " - " << arg_n_info();
                    output_usage_verbose(obj);
                }
            }
            return false;
        }
        // check extra arguments
        if (left_args.size()) {
            if (print_err) {
                Error(obj) << "[rotate( " << left_args.size() << " unexpected extra arguments were found: " << left_args;
                output_usage(obj);
            }
            return false;
        }
        return true;
    }
};

const char* m_rotate_args_info() {
    return "rotate internal pixel buffer";
}
void m_rotate_args_info_output(const BaseObject* obj) {
    logpost(obj ? static_cast<void*>(obj->owner()) : nullptr,
        PD_NORMAL, "%s", m_rotate_args_info());
}
} // namespace 

#endif // HW_RPI_SPI_WS2812_ARGS_HPP_
